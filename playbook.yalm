- hosts: localhost
    tasks:
        - name: pull imagem do docker
            docker_image:
                name: python:3-slim
        - name: build imagem
            docker_image:
                build:
                    path: ./